export * from './render'

import {
  patchBaseVisualConfig,
  patchBaseAudioConfig,
  patchBaseTextConfig,
} from './render'

export const TimelineItemMutations = {
  patchBaseVisualConfig,
  patchBaseAudioConfig,
  patchBaseTextConfig,
}
