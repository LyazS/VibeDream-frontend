{
  "id": "ltx-2",
  "variant": "i2v",
  "web_app_id": 45539,
  "name": "ltx-2-i2v",
  "description": "使用LTX-2模型进行图片转视频，根据图片和提示词生成高质量视频",
  "media_type": "video",
  "cost": "0.3",
  "input_mapping": {
    "ref_images": {
      "type": "array",
      "items": [
        {
          "mapping": "98:LoadImage.image",
          "type": "string",
          "default": "",
          "validation": {
            "required": true,
            "description": "参考图片URL"
          }
        }
      ]
    },
    "prompt": {
      "mapping": "118:CLIPTextEncode.text",
      "type": "string",
      "default": "An elegant Chinese woman was running swiftly through a peaceful garden bathed in the lingering glow of the setting sun. The garden, now soaked by the rain, appeared exceptionally serene in the twilight. She was wearing a flowing cheongsam with exquisite embroidery and held a transparent paper umbrella. Raindrops fell one by one. The lights were soft, with a cinematic quality, and the scene presented a slight slow-motion effect, giving a slightly impressionist visual effect.\nSound effect: Only the sound of rain.",
      "validation": {
        "required": true,
        "minLength": 1,
        "maxLength": 5000,
        "description": "生成提示词，描述要生成的视频内容"
      }
    },
    "negative_prompt": {
      "mapping": "113:CLIPTextEncode.text",
      "type": "string",
      "default": "blurry, low quality, still frame, frames, watermark, overlay, titles, has blurbox, has subtitles, has music",
      "validation": {
        "required": false,
        "maxLength": 5000,
        "description": "负面提示词，描述不希望出现的内容"
      }
    },
    "aspect_ratio": {
      "mapping": "aspect_ratio",
      "type": "option",
      "default": "16:9",
      "validation": {
        "required": false,
        "description": "宽高比（如 16:9, 9:16, 1:1, 4:3, 3:4, 21:9）",
        "option": [
          "16:9",
          "9:16",
          "1:1",
          "4:3",
          "3:4",
          "21:9"
        ]
      },
      "skip_mapping": true
    },
    "width": {
      "mapping": "146:ImageResizeKJv2.width",
      "type": "integer",
      "default": 1280,
      "validation": {
        "required": false,
        "min": 256,
        "max": 1920,
        "computed": true,
        "description": "视频宽度（由 aspect_ratio 自动计算）"
      }
    },
    "height": {
      "mapping": "146:ImageResizeKJv2.height",
      "type": "integer",
      "default": 720,
      "validation": {
        "required": false,
        "min": 256,
        "max": 1080,
        "computed": true,
        "description": "视频高度（由 aspect_ratio 自动计算）"
      }
    },
    "duration": {
      "mapping": "150:INTConstant.value",
      "type": "integer",
      "default": 5,
      "validation": {
        "required": false,
        "min": 1,
        "max": 20,
        "description": "视频时长（秒）"
      }
    }
  }
}
